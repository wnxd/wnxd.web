(function () { function JSONP(domain, data) { if (data != null) { var script = document.createElement("script"); script.type = "text/javascript"; if (domain.substring(domain.length - 1) != "/") domain += "/"; script.src = domain + "?wnxd_cookie=sync&list=" + encodeURIComponent(JSON.stringify(data)); document.body.appendChild(script) } } var list = $$$; if (list) for (var key in list) JSONP(key, list[key]) })();